# Admin panel environment. Copy to `.env` (or set in Vercel project settings).
# Same Supabase project as the TV app. The anon key is safe to expose (RLS-gated).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
