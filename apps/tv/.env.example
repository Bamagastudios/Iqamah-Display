# TV app environment. Copy to `.env` and adjust as needed.

# Prayer-times API (consumed as-is). Defaults to this if unset.
VITE_PRAYER_API=https://tajweed-website-beige.vercel.app/api/prayer-times

# Supabase content backend — added in Phase 4 (read-only anon key, safe to ship).
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
